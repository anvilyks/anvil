/* ——— Nagłówek: tytuł u góry, filtr na dole ——— */

/* 1) Komórka nagłówka może rosnąć i układa się pionowo */
.tabulator .tabulator-header .tabulator-col{
  display: flex;
  flex-direction: column;
  height: auto;           /* pozwól rosnąć przy długich tytułach */
  min-height: 64px;       /* zapewnij miejsce, by filtr miał gdzie "siąść" */
}

/* 2) Wnętrze nagłówka też pionowo i wypełnia wysokość komórki */
.tabulator .tabulator-header .tabulator-col .tabulator-col-content{
  display: flex;
  flex-direction: column; /* tytuł u góry */
  flex: 1 1 auto;         /* rozciągnij na całą wysokość */
  min-height: 0;
}

/* 3) Tytuł może się zawijać; strzałka sortowania przy górnej krawędzi */
.tabulator .tabulator-header .tabulator-col .tabulator-col-title{
  white-space: normal;
  word-break: break-word;
  line-height: 1.2;
}
.tabulator .tabulator-header .tabulator-col .tabulator-col-title-holder{
  display: flex;
  align-items: flex-start; /* sorter u góry, nie pcha filtra */
  gap: 6px;
}

/* 4) KLUCZ: filtr zawsze na dole (obejmuje oba warianty DOM Tabulatora) */
.tabulator .tabulator-header .tabulator-col .tabulator-col-content > .tabulator-header-filter,
.tabulator .tabulator-header .tabulator-col > .tabulator-header-filter{
  margin-top: auto;        /* dociśnij filtr do dołu */
}

/* 5) Ładny, równy wygląd pól filtrów (opcjonalnie) */
.tabulator .tabulator-header .tabulator-col .tabulator-header-filter input,
.tabulator .tabulator-header .tabulator-col .tabulator-header-filter select{
  width: 100%;
  box-sizing: border-box;
  height: 28px;
}
